# 등급 포인트 시스템 설계안

## 📋 개요
자영업자 커뮤니티에서 사용자의 활동을 기반으로 포인트를 적립하고 등급을 부여하는 시스템입니다.

## 🎯 핵심 목표

1. **사용자 참여 유도**: 활발한 활동을 장려
2. **커뮤니티 품질 향상**: 유용한 콘텐츠 생산 유도
3. **신뢰도 표시**: 등급으로 사용자 신뢰도 시각화
4. **게임화 요소**: 재미있는 경험 제공

## 💎 포인트 적립 시스템

### 포인트 적립 항목

```
📝 게시글 관련
- 게시글 작성: +10 포인트
- 게시글 좋아요 받기: +2 포인트 (1개당)
- 게시글 댓글 받기: +1 포인트 (1개당)
- 베스트 게시글 선정: +50 포인트

💬 댓글 관련
- 댓글 작성: +5 포인트
- 댓글 좋아요 받기: +1 포인트 (1개당)

📨 소통 관련
- 쪽지 보내기: +1 포인트
- 쪽지 받기: +0 포인트 (수동적)

✅ 인증 관련
- 사업자 인증 완료: +100 포인트
- 프로필 완성도 100%: +20 포인트

⭐ 특별 활동
- 일일 로그인: +5 포인트 (하루 1회)
- 연속 로그인: +10 포인트 (7일 연속)
- 첫 게시글 작성: +20 포인트
- 첫 댓글 작성: +10 포인트
```

### 포인트 차감 항목

```
⚠️ 부정 행위
- 게시글 신고 받기: -10 포인트
- 댓글 신고 받기: -5 포인트
- 관리자 경고: -50 포인트
- 계정 정지: -200 포인트
```

## 🏆 등급 시스템

### 등급별 기준

```
🌱 새싹 사장님 (0-99 포인트)
- 아이콘: 🌱
- 색상: #10B981 (그린)
- 혜택: 기본 기능 사용

🌿 성장 사장님 (100-299 포인트)
- 아이콘: 🌿
- 색상: #3B82F6 (블루)
- 혜택: 베스트 게시글 추천 우선권

🌳 베테랑 사장님 (300-599 포인트)
- 아이콘: 🌳
- 색상: #8B5CF6 (퍼플)
- 혜택: 프로필 배지 표시, 게시글 상단 노출

🎋 대나무 사장님 (600-999 포인트)
- 아이콘: 🎋
- 색상: #F59E0B (오렌지)
- 혜택: 특별 배지, 우선 고객 지원

💎 다이아몬드 사장님 (1000+ 포인트)
- 아이콘: 💎
- 색상: #FFBF00 (골드)
- 혜택: VIP 배지, 모든 기능 우선권
```

## 📊 데이터 구조

### Firestore: `users` 컬렉션 확장
```typescript
{
  // 기존 필드들...
  points: number                    // 총 포인트
  level: string                     // 현재 등급
  totalPosts: number                // 총 게시글 수
  totalComments: number             // 총 댓글 수
  totalLikes: number                // 받은 좋아요 수
  loginStreak: number               // 연속 로그인 일수
  lastLoginDate: string             // 마지막 로그인 날짜
  dailyLoginBonus: boolean          // 오늘 일일 로그인 보너스 받았는지
  achievements: string[]            // 달성한 업적 목록
  pointsHistory: Array<{            // 포인트 내역
    points: number
    reason: string
    timestamp: Timestamp
    type: 'earn' | 'deduct'
  }>
}
```

### Firestore: `points_log` 컬렉션 (선택사항)
```typescript
{
  userId: string
  points: number
  reason: string
  type: 'earn' | 'deduct'
  relatedId?: string                // 관련 게시글/댓글 ID
  relatedType?: 'post' | 'comment' | 'like' | 'login' | 'verify'
  timestamp: Timestamp
}
```

## 🎮 업적 시스템

### 업적 목록

```
🏅 첫 발걸음
- 첫 게시글 작성
- 보상: +20 포인트

🏅 소통의 시작
- 첫 댓글 작성
- 보상: +10 포인트

🏅 인기 작가
- 게시글 좋아요 100개 달성
- 보상: +100 포인트

🏅 베스트 작가
- 베스트 게시글 3개 작성
- 보상: +150 포인트

🏅 열정 사장님
- 일주일 연속 로그인
- 보상: +50 포인트

🏅 인증 사장님
- 사업자 인증 완료
- 보상: +100 포인트

🏅 커뮤니티 스타
- 댓글 100개 작성
- 보상: +200 포인트

🏅 대나무숲의 별
- 포인트 1000점 달성
- 보상: 특별 배지
```

## 🎨 UI/UX 디자인

### 프로필 카드에 등급 표시
```
┌─────────────────────────────────┐
│  👤 사용자명                    │
│  💎 다이아몬드 사장님           │
│  포인트: 1,250점                │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  다음 등급까지: 250점            │
└─────────────────────────────────┘
```

### 게시글 카드에 등급 배지
```
┌─────────────────────────────────┐
│  💎 다이아몬드 사장님            │
│  제목: ...                       │
│  내용: ...                       │
└─────────────────────────────────┘
```

### 포인트 내역 페이지 (`/mypage/points`)
```
┌─────────────────────────────────┐
│  📊 포인트 내역                  │
├─────────────────────────────────┤
│  +10  게시글 작성                │
│      2025.01.15 14:30            │
├─────────────────────────────────┤
│  +2   좋아요 받음                │
│      2025.01.15 13:20            │
├─────────────────────────────────┤
│  +5   일일 로그인 보너스         │
│      2025.01.15 09:00            │
└─────────────────────────────────┘
```

## 🛠️ 기술 구현

### 1. 포인트 적립 함수

```typescript
// lib/points.ts
export async function addPoints(
  userId: string,
  points: number,
  reason: string,
  relatedId?: string,
  relatedType?: string
) {
  if (!db) return

  const userRef = doc(db, 'users', userId)
  const userSnap = await getDoc(userRef)
  
  if (!userSnap.exists()) return

  const currentPoints = userSnap.data().points || 0
  const newPoints = currentPoints + points
  const newLevel = calculateLevel(newPoints)

  // 포인트 히스토리 추가
  const pointsHistory = userSnap.data().pointsHistory || []
  pointsHistory.push({
    points,
    reason,
    timestamp: serverTimestamp(),
    type: points > 0 ? 'earn' : 'deduct',
  })

  await updateDoc(userRef, {
    points: newPoints,
    level: newLevel,
    pointsHistory: pointsHistory.slice(-100), // 최근 100개만 저장
  })

  // 포인트 로그 저장 (선택사항)
  await addDoc(collection(db, 'points_log'), {
    userId,
    points,
    reason,
    type: points > 0 ? 'earn' : 'deduct',
    relatedId,
    relatedType,
    timestamp: serverTimestamp(),
  })

  // 등급 업그레이드 체크
  if (newLevel !== userSnap.data().level) {
    // 등급 업그레이드 알림
    return { upgraded: true, newLevel }
  }

  return { upgraded: false }
}

function calculateLevel(points: number): string {
  if (points >= 1000) return '다이아몬드 사장님'
  if (points >= 600) return '대나무 사장님'
  if (points >= 300) return '베테랑 사장님'
  if (points >= 100) return '성장 사장님'
  return '새싹 사장님'
}
```

### 2. 게시글 작성 시 포인트 적립

```typescript
// app/page.tsx 또는 WriteModal.tsx
import { addPoints } from '@/lib/points'

// 게시글 작성 후
await addDoc(collection(db, 'posts'), { ... })

// 포인트 적립
await addPoints(user.uid, 10, '게시글 작성', postId, 'post')
```

### 3. 좋아요 시 포인트 적립

```typescript
// app/post/[id]/page.tsx
const handleLike = async () => {
  // 좋아요 처리...
  
  // 작성자에게 포인트 적립
  if (post.uid !== user.uid) {
    await addPoints(post.uid, 2, '게시글 좋아요 받음', post.id, 'like')
  }
}
```

### 4. 일일 로그인 보너스

```typescript
// app/page.tsx 또는 layout.tsx
useEffect(() => {
  if (!user || !db) return

  const checkDailyLogin = async () => {
    const userRef = doc(db, 'users', user.uid)
    const userSnap = await getDoc(userRef)
    
    if (!userSnap.exists()) return

    const userData = userSnap.data()
    const today = new Date().toDateString()
    const lastLogin = userData.lastLoginDate

    if (lastLogin !== today) {
      // 연속 로그인 체크
      const yesterday = new Date()
      yesterday.setDate(yesterday.getDate() - 1)
      const isConsecutive = lastLogin === yesterday.toDateString()

      let points = 5 // 기본 일일 로그인
      let loginStreak = 1

      if (isConsecutive) {
        loginStreak = (userData.loginStreak || 0) + 1
        if (loginStreak >= 7) {
          points = 10 // 연속 로그인 보너스
        }
      }

      await updateDoc(userRef, {
        lastLoginDate: today,
        loginStreak,
        dailyLoginBonus: true,
      })

      await addPoints(user.uid, points, '일일 로그인 보너스')
    }
  }

  checkDailyLogin()
}, [user, db])
```

## 📈 통계 및 리더보드

### 리더보드 페이지 (`/leaderboard`)
```
┌─────────────────────────────────┐
│  🏆 포인트 랭킹                 │
├─────────────────────────────────┤
│  1위 💎 다이아몬드 사장님       │
│     1,250점                     │
├─────────────────────────────────┤
│  2위 🎋 대나무 사장님           │
│     850점                        │
├─────────────────────────────────┤
│  3위 🌳 베테랑 사장님           │
│     450점                        │
└─────────────────────────────────┘
```

## 🎁 등급별 혜택

### 새싹 사장님 (0-99)
- 기본 기능 사용
- 게시글 작성/댓글 작성

### 성장 사장님 (100-299)
- 베스트 게시글 추천 우선권
- 프로필 커스터마이징

### 베테랑 사장님 (300-599)
- 프로필 배지 표시
- 게시글 상단 노출 (일정 확률)
- 특별 이모지 사용

### 대나무 사장님 (600-999)
- 특별 배지
- 우선 고객 지원
- 베스트 게시글 자동 추천

### 다이아몬드 사장님 (1000+)
- VIP 배지
- 모든 기능 우선권
- 관리자와 직접 소통
- 특별 이벤트 초대

## 🔔 알림 시스템

### 등급 업그레이드 알림
```
┌─────────────────────────────────┐
│  🎉 축하합니다!                 │
│  🌳 베테랑 사장님으로           │
│     승급하셨습니다!             │
│  [확인]                         │
└─────────────────────────────────┘
```

### 포인트 적립 알림
```
┌─────────────────────────────────┐
│  +10 포인트 적립!               │
│  게시글 작성                    │
│  현재 포인트: 120점             │
└─────────────────────────────────┘
```

## 📱 구현 우선순위

### Phase 1 (필수)
1. ✅ 포인트 적립 시스템 (게시글, 댓글, 좋아요)
2. ✅ 등급 계산 및 표시
3. ✅ 프로필에 등급 표시
4. ✅ 게시글에 등급 배지 표시

### Phase 2 (중요)
5. ✅ 일일 로그인 보너스
6. ✅ 포인트 내역 페이지
7. ✅ 등급 업그레이드 알림
8. ✅ 업적 시스템

### Phase 3 (선택)
9. ⏳ 리더보드 페이지
10. ⏳ 등급별 혜택 시스템
11. ⏳ 포인트 선물 기능
12. ⏳ 포인트 상점 (아이템 구매)

## 🎯 사용자 경험 개선

### 포인트 적립 애니메이션
- 포인트 적립 시 플로팅 애니메이션
- 등급 업그레이드 시 축하 모달
- 포인트 진행 바 표시

### 동기부여 요소
- 다음 등급까지 남은 포인트 표시
- 주간/월간 포인트 목표
- 친구와 포인트 비교

## 🔒 보안 고려사항

### 포인트 조작 방지
- 서버 사이드에서만 포인트 적립
- 중복 적립 방지 (같은 게시글에 여러 번 좋아요 불가)
- 일일 포인트 한도 설정 (스팸 방지)

### 포인트 복구
- 잘못 차감된 포인트 복구 기능
- 관리자 포인트 조정 권한

## 📊 예상 효과

1. **사용자 참여도 증가**: 포인트 적립으로 활동 유도
2. **콘텐츠 품질 향상**: 좋은 글에 더 많은 포인트
3. **커뮤니티 활성화**: 등급 경쟁으로 활발한 소통
4. **사용자 유지율 증가**: 게임화 요소로 재방문 유도

## 💡 차별화 포인트

1. **자영업자 맞춤**: 사업자 인증 보너스 등 특화
2. **공정한 시스템**: 신고 시 포인트 차감으로 품질 관리
3. **시각적 피드백**: 등급 배지와 애니메이션
4. **지속적 동기부여**: 다음 등급까지 진행률 표시

